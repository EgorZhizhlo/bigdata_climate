# 📄 **README.md**

# **Проект: Анализ климатических данных (Big Data)**

Этот проект реализует распределённый ETL-пайплайн для анализа климатических данных из локальных CSV-файлов. В работе используются:
**Prefect 2.x** для оркестрации,
**Dask** для параллельной обработки,
**PostgreSQL** как хранилище,
**Jupyter Notebook** для визуализации и анализа.

---

# 📘 **1. Структура данных**

Каждый CSV состоит из **двух логических частей**:

## **1.1. Блок информации для идентификации локации**

Этот блок содержит метаданные об уникальной географической точке:

```
location_id,latitude,longitude,elevation,utc_offset_seconds,timezone,timezone_abbreviation
0,55.782074,37.576374,141.0,0,GMT,GMT
1,59.9297,30.416666,4.0,0,GMT,GMT
2,55.782074,49.124237,70.0,0,GMT,GMT
...
```

### ❗ Важное правило:

* `location_id` **внутренний идентификатор** внутри конкретного файла.
* **Не является глобальным!**
* `location_id = 0` в файле A и `location_id = 0` в файле B — **две разные локации**.
* Истинная уникальность достигается с помощью пары:

```
(latitude, longitude)
```

Эту пару мы используем как уникальный природный ключ при загрузке данных.

---

## **1.2. Блок климатических временных рядов**

После блока метаданных идут записи измерений по времени:

```
location_id,time,temperature_2m (°C),relative_humidity_2m (%),precipitation (mm),...
0,2000-01-03T00:00,-10.3,90,0.00,-14.3,-11.6,0.00,0.19,0.00,...
0,2000-01-03T01:00,-10.2,90,0.00,-14.1,-11.5,0.00,0.19,0.00,...
...
```

Каждая строка — это измерение погоды в момент `time` для определённой геолокации.

---

# 📁 **2. Структура проекта**

```
📁 bigdata_climate/
 ┣ 📂 data_raw/               # локальные CSV-файлы
 │   ┣ file1.csv
 │   ┣ file2.csv
 │   ┣ file3.csv
 │   ┗ file4.csv
 ┣ 📂 flows/                  # Prefect ETL flows
 ┣ 📂 dask_jobs/              # Dask скрипты параллельной обработки
 ┣ 📂 notebooks/              # Jupyter ноутбуки для анализа
 ┣ 📂 db/                     # SQL схемы, миграции
 ┣ 📄 docker-compose.yml      # PostgreSQL (или другие сервисы при необходимости)
 ┣ 📄 requirements.txt        # зависимости проекта
 ┣ 📄 README.md
 ┗ 📄 report.docx             # отчёт
```

---

# 🔧 **3. Используемые технологии**

### **Big Data / обработка**

* **Dask** — параллельная обработка и вычисления
* **Pandas + NumPy** — базовая обработка таблиц

### **Оркестрация**

* **Prefect 2.x**

### **Хранилище**

* **PostgreSQL 16+**

### **Аналитика и визуализация**

* **Jupyter Notebook**
* **Matplotlib**
* **Seaborn**

---

# 🔁 **. ETL-пайплайн (Prefect)**

### **ETL включает 3 этапа:**

---

## **6.1 Extract**

* Чтение всех CSV из `data_raw/`
* Разделение файла на два блока:

  * метаданные локаций
  * временные ряды
* Привязка локаций по `(latitude, longitude)`
* Нормализация столбцов
* Первичная проверка данных

---

## **6.2 Transform (через Dask)**

* удаление дубликатов
* корректировка форматов времени
* заполнение пропусков
* генерация признаков:

  * год / месяц / день
  * индикаторы аномалий
* агрегации:

  * средняя температура по месяцам
  * суммарные осадки
  * годовые экстремумы

---

## **6.3 Load**

Результаты загружаются в PostgreSQL в таблицы:

**1) Локации (unique key = latitude+longitude)**

```
locations(id, latitude, longitude, elevation, timezone, ...)
```

**2) raw_climate** — сырые временные ряды
**3) climate_metrics** — агрегированные метрики

---

# 📊 **7. Аналитика (Jupyter)**

Используются ноутбуки:

### `01_raw_analysis.ipynb`

* просмотр сырых CSV
* проверка пропусков
* распределения температур

### `02_aggregations.ipynb`

* график средней температуры по месяцам
* суммарные осадки по годам

### `03_correlations.ipynb`

* корреляции между погодными факторами

### `04_final_plots.ipynb`

* графики для презентации и отчёта

---

# 📈 **8. Цели анализа**

В проекте вычисляются:

* тренды температуры
* интенсивность осадков
* экстремальные погодные явления
* анализ сезонности
* корреляции (температура ↔ влажность ↔ давление)

---

# 📑 **9. Как запустить проект**

1. Установить зависимости

2. Запустить PostgreSQL:

```
docker-compose up -d
```

3. Запустить Prefect UI:

```
prefect server start
```

4. Запустить ETL Flow:

```
python flows/extract.py
python flows/transform.py
python flows/load.py
```

5. Открыть Jupyter:

```
jupyter notebook
```

---

## ⚙️ Быстрый старт инфраструктуры

1. Скопируй `.env.example` → `.env` и при необходимости поправь `POSTGRES_DSN` / пути.
2. Запусти инфраструктуру: `docker-compose up -d postgres pgadmin prefect-server prefect-agent jupyter dask-scheduler dask-worker-1 dask-worker-2 dask-worker-3`.
3. Помести CSV (~1 ГБ) в `data_raw/` — они автоматически пробрасываются в контейнеры.
4. Запусти моковый Prefect flow прямо на хосте: `python flows/etl_flow.py`. Хочешь выполнить его из контейнера — примонтируй репозиторий (например, `./:/app`) к сервису `prefect-agent` и используй команду `docker-compose run --rm prefect-agent python /app/flows/etl_flow.py`.
5. Просмотри результаты:
   * PostgreSQL: таблица `climate.climate_metrics_staging` (или parquet в `data_processed/`).
   * Файл `data_processed/dask_describe.csv` после запуска `python dask_jobs/sample_metrics.py --sample-frac 0.05` (по умолчанию Dask-воркеры читают CSV из `/data_raw`, можно переопределить через `DASK_WORKER_DATA_RAW_DIR`).

Готово — инфраструктура функционирует и можно переходить к реализации полноценного ETL.

## ✅ Проверка стиля кода

1. Установи dev-зависимости (`pip install pytest flake8` или добавь их в своё окружение).
2. Запусти `pytest tests/test_flake8.py` — внутри этот тест вызывает `python -m flake8` и падает при первых стилевых ошибках.
3. В VS Code тест автоматически появляется в Test Explorer, потому что проект содержит готовую конфигурацию в `.vscode/`.

# 📝 **10. Отчёт и презентация**

В корне проекта есть:

* **report.docx** — текст отчёта
* **slides/** (если требуется)

---



Вот **полностью обновлённый, финальный и профессионально оформленный README.md**, учитывающий:

✔ твой полный стек в Docker Compose
✔ использование JupyterLab в контейнере
✔ Dask Scheduler + 3 Workers
✔ Prefect Server + Prefect Agent
✔ PostgreSQL + pgAdmin
✔ локальные CSV (1 ГБ)
✔ уникальность локаций через `(latitude, longitude)`
✔ двухслойную структуру каждого CSV
✔ структуру проекта, пайплайнов и всего процесса

Готов класть в корень проекта **как финальный README.md**.

---

# 📊 **Big Data Climate Analysis Project**

## **Distributed Climate Data Processing System Based on Prefect, Dask, PostgreSQL, Jupyter, Docker**

Этот проект реализует **распределённую систему обработки климатических данных** на основе современных технологий Big Data.
Вся инфраструктура полностью развёрнута в **Docker Compose** и включает:

* **Dask (Scheduler + 3 Workers)** — параллельная обработка климатических данных
* **Prefect (Server + Agent)** — оркестрация ETL
* **PostgreSQL** — хранилище
* **pgAdmin** — управление БД
* **JupyterLab** — аналитика, визуализация и исследование данных
* **Общий volume с CSV** — ~1 ГБ данных

Проект демонстрирует **полный цикл обработки данных**: получение → очистка → агрегации → хранение → анализ.

---

# 📁 **Структура данных**

Каждый CSV-файл (всего 1 ГБ данных) содержит **две логические части**:

---

## **1. Блок метаданных о локации**

Каждая строка — описание географической точки:

| Column                  | Description                                        |
| ----------------------- | -------------------------------------------------- |
| `location_id`           | *локальный ID, уникален только внутри данного CSV* |
| `latitude`              | Широта                                             |
| `longitude`             | Долгота                                            |
| `elevation`             | Высота                                             |
| `utc_offset_seconds`    | UTC-смещение                                       |
| `timezone`              | Часовой пояс                                       |
| `timezone_abbreviation` | Сокращение                                         |

Пример:

```
location_id,latitude,longitude,elevation,utc_offset_seconds,timezone,timezone_abbreviation
0,55.782074,37.576374,141.0,0,GMT,GMT
1,59.9297,30.416666,4.0,0,GMT,GMT
2,55.782074,49.124237,70.0,0,GMT,GMT
```

### ❗ Важное правило уникальности:

* `location_id` **НЕ является глобальным идентификатором**
* Настоящая уникальность достигается через:

```
(latitude, longitude)
```

---

## **2. Блок временных рядов погоды**

Каждая строка — измерение для конкретной локации в момент времени:

| Column                 | Description                        |
| ---------------------- | ---------------------------------- |
| `time`                 | Время измерения                    |
| `temperature_2m`       | Температура                        |
| `relative_humidity_2m` | Влажность                          |
| `precipitation`        | Осадки                             |
| `dew_point_2m`         | Точка росы                         |
| `wind_speed_10m`, etc. | Скорость ветра                     |
| …                      | Более 20 дополнительных параметров |

Пример:

```
0,2000-01-03T00:00,-10.3,90,0.00,-14.3,-11.6,0.00,0.19,0.00,...
0,2000-01-03T01:00,-10.2,90,0.00,-14.1,-11.5,0.00,0.19,0.00,...
```

---

# 🌐 **Полезные ссылки**

| Компонент      | URL                                            |
| -------------- | ---------------------------------------------- |
| Jupyter        | [http://localhost:8888](http://localhost:8888) |
| Prefect UI     | [http://localhost:4200](http://localhost:4200) |
| pgAdmin        | [http://localhost:5050](http://localhost:5050) |
| Dask Dashboard | [http://localhost:8787](http://localhost:8787) |

---
